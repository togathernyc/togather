// Settings feature types
// Types specific to the settings feature

export interface SettingsFormData {
  first_name: string;
  last_name: string;
}

export interface Settings {
  first_name: string;
  last_name: string;
  email: string;
}
