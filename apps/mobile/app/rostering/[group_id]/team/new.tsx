import { TeamCreateScreen } from "@features/scheduling";

/** Create-team flow — route `/rostering/[group_id]/team/new`. See ADR-025. */
export default function RosteringTeamCreatePage() {
  return <TeamCreateScreen />;
}
