import { RosteringTeamsScreen } from "@features/scheduling";

/** Teams tab of the Rostering hub — route `/rostering/[group_id]/teams`. */
export default function RosteringTeamsTab() {
  return <RosteringTeamsScreen />;
}
