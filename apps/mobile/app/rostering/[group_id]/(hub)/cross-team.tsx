import { RosteringCrossTeamScreen } from "@features/scheduling";

/** Cross-team tab of the Rostering hub — route `/rostering/[group_id]/cross-team`. */
export default function RosteringCrossTeamTab() {
  return <RosteringCrossTeamScreen />;
}
