import { MyAvailabilityScreen } from "@features/scheduling";

/** My Availability screen — route `/rostering/[group_id]/availability`. */
export default function MyAvailabilityRoute() {
  return <MyAvailabilityScreen />;
}
