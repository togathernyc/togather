import { RunSheetScreen } from "@features/scheduling";

/** Native run sheet (order-of-items) for an event plan. See ADR-026. */
export default function RunSheetPage() {
  return <RunSheetScreen />;
}
