"use client";

/**
 * Community Landing Page Route
 *
 * Public page for newcomers to submit their info and join a community.
 * Accessed via /c/[slug] (e.g., /c/fount).
 */

import CommunityLandingPageClient from "./CommunityLandingPageClient";

export default function CommunityLandingPage() {
  return <CommunityLandingPageClient />;
}
