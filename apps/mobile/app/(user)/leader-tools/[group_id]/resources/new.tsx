/**
 * New Resource Route
 *
 * Route file for creating a new resource.
 * Delegates to ResourceToolSettings component which handles both create and edit modes.
 */
import { ResourceToolSettings } from "@features/leader-tools/components/ResourceToolSettings";

export default ResourceToolSettings;
