"use client";

/**
 * Channel Invite Page Route Component
 *
 * Handles CLIENT-SIDE routing for channel invite links.
 */

import ChannelInvitePageClient from "./ChannelInvitePageClient";

export default function ChannelInvitePage() {
  return <ChannelInvitePageClient />;
}
